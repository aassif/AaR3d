# Script generated by PGen.
# ------------------------------
# Build script for AaR3d project.
# Date : dim. 25. juil. 00:47:44 2010.

# Required CMake version.
cmake_minimum_required(VERSION 2.8)

# Project name.
project(AaR3d C CXX)
set(CMAKE_CXX_FLAGS "/EHa")

# Module directory.
set(CMAKE_MODULE_PATH $ENV{TLR_DEV}/cmake-modules)
STRING(REGEX REPLACE "\\\\" "/" CMAKE_MODULE_PATH ${CMAKE_MODULE_PATH})

# OpenGL dependency.
find_package(OpenGL REQUIRED) # GLEW

# GLEW dependency.
set(GLEW_DIR $ENV{TLR_DEV}/glew-1.7.0)
find_package(GLEW REQUIRED) # AaOpenGL

# AaToolkit dependency
set(AATOOLKIT_DIR $ENV{TLR_DEV}/aassif-toolkit)
find_package(AaToolkit REQUIRED) # AaOpenGL

# AaOpenGL dependency
set(AAOPENGL_DIR $ENV{TLR_DEV}/aassif-opengl)
find_package(AaOpenGL REQUIRED)

# AaMesh dependency
set(AAMESH_DIR $ENV{TLR_DEV}/aassif-mesh)
find_package(AaMesh REQUIRED)

# Include directoires.
include_directories(
  include
  ${AAMESH_INCLUDE_DIR}
  ${OPENGL_INCLUDE_DIR}
  ${GLEW_INCLUDE_DIR}
  ${AATOOLKIT_INCLUDE_DIR}
  ${AAOPENGL_INCLUDE_DIR}
)

# Include files.
set(PROJECT_HDR
  include/AaR3d.h
  include/R3dImage.h
  include/R3dImageRenderer.h
  include/R3dImageRenderer2d.h
  include/R3dImageRenderer3d.h
  include/R3dImageRenderer3dGLSL.h
  include/R3dLut.h
)

# Source files.
set(PROJECT_SRC
  src/R3dImage.cc
  src/R3dImageRenderer.cc
  src/R3dImageRenderer2d.cc
  src/R3dImageRenderer3d.cc
  src/R3dImageRenderer3dGLSL.cc
  src/R3dLut.cc
)

# Target generation.
add_library(AaR3d
  SHARED
  ${PROJECT_HDR}
  ${PROJECT_SRC}
)

# Output paths.
set_target_properties(
  AaR3d
  PROPERTIES
  RUNTIME_OUTPUT_DIRECTORY_DEBUG   ${PROJECT_SOURCE_DIR}/bin/debug
  RUNTIME_OUTPUT_DIRECTORY_RELEASE ${PROJECT_SOURCE_DIR}/bin/release
  LIBRARY_OUTPUT_DIRECTORY_DEBUG   ${PROJECT_SOURCE_DIR}/lib/debug
  LIBRARY_OUTPUT_DIRECTORY_RELEASE ${PROJECT_SOURCE_DIR}/lib/release
  ARCHIVE_OUTPUT_DIRECTORY_DEBUG   ${PROJECT_SOURCE_DIR}/lib/debug
  ARCHIVE_OUTPUT_DIRECTORY_RELEASE ${PROJECT_SOURCE_DIR}/lib/release
  DEBUG_POSTFIX d
)

# Target dependencies.
target_link_libraries(AaR3d
  ${OPENGL_LIBRARY}
  ${GLEW_LIBRARY}
  ${AATOOLKIT_LIBRARY}
  ${AAOPENGL_LIBRARY}
)

# Project dependencies.
#add_dependencies(AaR3d
#  AaToolkit
#)
